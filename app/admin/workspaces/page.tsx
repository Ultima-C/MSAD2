"use client";

// 1. Import it as WorkspacesSection
import WorkspacesPage from "@/components/admin_dashboard/sections/workspaces";

// 2. Export the page as WorkspacesPage
export default function Workspaces() {
  // 3. Render the Section
  return <WorkspacesPage />;
}
